Patch the input focus states on the Sell page to ensure a true 'primary-glow' effect.

TARGET: /app/app/sell/page.tsx

ISSUE:
The `cinematicInputClass` currently applies a primary border color on focus but lacks an outer luminescence/glow, which is a requirement for the 'Premium Cinematic SaaS' doctrine.

REQUIRED CHANGE:
Update the `cinematicInputClass` variable to include an outer box-shadow on focus.

OLD STRING:
'border-white/10 bg-black/40 text-white shadow-[inset_0_2px_4px_rgba(0,0,0,0.4)] transition-all placeholder:text-white/25 focus:border-primary/40 focus:bg-[#0A0A0B] focus-visible:ring-0'

NEW STRING:
'border-white/10 bg-black/40 text-white shadow-[inset_0_2px_4px_rgba(0,0,0,0.4)] transition-all placeholder:text-white/25 focus:border-primary/40 focus:bg-[#0A0A0B] focus:shadow-[0_0_12px_rgba(var(--primary),0.2)] focus-visible:ring-0'

CODEX DIRECTIVE:
- Only modify the `cinematicInputClass` string.
- Do not touch any other logic or layout.
- Verify that the resulting class string is syntactically correct Tailwind CSS.