'use client';

import AppDashboard from '@/app/app/page';
import AppLayout from '@/app/app/layout';

export default function DiscoverV7PreviewPage() {
  return (
    <AppLayout>
      <AppDashboard />
    </AppLayout>
  );
}
